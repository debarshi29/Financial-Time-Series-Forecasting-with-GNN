# Unified requirements for all submodules (THGNN, THGNN_MaGNet, THGNN_Mamba_MoE, MultiAgent, demo)
# Install with:  pip install -r requirements.txt
# For CUDA 12.4:  pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu124

# --- PyTorch (CPU default; Docker image supplies CUDA build) ---
torch>=2.5.1
torchvision>=0.20.1
torchaudio>=2.5.1

# --- Core data / ML ---
numpy>=1.26.0
pandas>=2.2.3
scipy>=1.13.0
scikit-learn>=1.3.0
networkx>=3.4

# --- Finance data ---
yfinance>=1.0
requests>=2.32.5

# --- Visualisation ---
matplotlib>=3.8.0
seaborn>=0.13.2

# --- Utilities ---
tqdm>=4.66.0

# --- Demo API server ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.9

# --- MultiAgent / LLM pipeline ---
langgraph>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.0
langchain-google-genai>=1.0.0
transformers>=4.40.0

# --- Streamlit dashboard (MultiAgent) ---
streamlit>=1.35.0

# --- Optional: true Mamba SSM (Linux + CUDA only) ---
# mamba-ssm>=1.2.0
